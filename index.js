/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SignedInStack from './screens/navigation';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SignedInStack);
